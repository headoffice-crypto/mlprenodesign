# API keys
staging/tools/js/config.js

# Windows shortcuts
*.lnk

# Supabase CLI scratch
staging/tools/supabase/.temp/
staging/tools/supabase/supabase/
supabase/.temp/

# Renovation portfolio assets (large media, kept local)
Renovation Portfolio/

# Promo video build artifacts + licensed music
promo/tmp/
promo/promo-*.mp4
promo/music.mp3
site/promo/promo-*.mp4
site/promo/music.mp3

# Microsoft Windows fonts (can't be redistributed — local copy only)
promo/fonts/georgiab.ttf
promo/fonts/segoeuib.ttf
promo/fonts/consola.ttf

# Rue-du-Havre raw footage — kept local until trimmed/encoded
site/event-media/rue-du-havre/

# Claude Code local settings
.claude/

.vercel

.vercel

# Test: also ignore site/public locally so file system doesn't matter
site/public/

# Recorder dependencies — Playwright + Chromium pulled by `npm install` in /promo
promo/node_modules/

# Loose root-level public/ — Vercel auto-detects this as a static-output folder
# and breaks production. Keep any QR assets under site/ instead.
public/
